// TODO:Backend::Auto Setup Pagination - when developer want to use pagination, they can just add a decorator to the method in the controller. It will automatically setup pagination for the method. No need to setup pagination in the controller manually, Or need to implement a pagination limit or offset in the controller or service.Decorator need sone props such as resource, route, transform callback, etc.

//
