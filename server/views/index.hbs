{{#extend 'body'}}
  <style>
    .custom-height { height: calc(100vh - 112px); }
  </style>
  <section class='custom-height'>
    <div class='bg-slate-100 h-full'>
      <h1 class='text-4xl text-center p-4 text-danger'>Welcome to {{title}}</h1>
      <p class='text-center p-4'>{{message}}</p>
    </div>
  </section>
{{/extend}}